# --- Node / Express ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Keep your lockfile committed (package-lock.json / yarn.lock), so NOT ignored.

# --- Environment / secrets ---
.env
.env.*
!.env.example          # keep a template in repo
*.local

# --- Logs & run artifacts ---
logs/
*.log
*.log.*
pids/
*.pid
*.pid.lock

# --- Build / coverage / caches ---
dist/
build/
coverage/
.nyc_output/
.tmp/
tmp/
temp/
.cache/
node_modules/.cache/

# --- Editors / OS cruft ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# --- TypeScript metadata (if you add TS later) ---
*.tsbuildinfo
